package com.digitalvault.digital_vault_backend.security;

import org.springframework.context.annotation.Configuration;

@Configuration
public class SecurityConfig {
    // Security configuration temporarily disabled for compilation
    // Will be properly configured once Spring Security dependencies are resolved
}
